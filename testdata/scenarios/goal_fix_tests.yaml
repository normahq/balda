name: goal_fix_tests
ingress: goal.inbound
goal:
  objective: fix failing tests in runtime runtime
  max_iterations: 3
expected:
  command_subject: balda.v1.cmd.job
  agent_flow:
    - planner
    - executor
    - reviewer
